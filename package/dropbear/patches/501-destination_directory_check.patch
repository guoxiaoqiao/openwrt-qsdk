--- a/scp.c
+++ b/scp.c
@@ -414,6 +414,11 @@ main(int argc, char **argv)
 	}
 	if (argc < 2)
 		usage();
+        if(argc == 2)
+        {
+                if(argv[1][strlen(argv[1])-1] == '/')
+                        targetshouldbedirectory = 1;
+        }
 	if (argc > 2)
 		targetshouldbedirectory = 1;
 
