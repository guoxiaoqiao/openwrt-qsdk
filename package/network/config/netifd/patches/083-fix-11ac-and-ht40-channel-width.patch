--- a/scripts/netifd-wireless.sh
+++ b/scripts/netifd-wireless.sh
@@ -58,13 +58,13 @@ _wdev_prepare_channel() {
 		;;
 	esac
 
-	[[ "$hwmode_n" = "$hwmode" ]] || {
+	[ "$hwmode_n" = "$hwmode"  ] && [ "$hwmode" != "ac" ] || {
 		enable_ht=1
 		hwmode="$hwmode_n"
 
 		json_get_vars htmode
 		case "$htmode" in
-			HT20|HT40+|HT40-);;
+			HT20|HT40|HT40+|HT40-);;
 			*) htmode= ;;
 		esac
 	}
